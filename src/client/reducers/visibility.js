const defaultVisibility = {
  income: false,
  outcome: false
}

const reducer = (state = defaultVisibility, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
